google-adk==2.0.0
google-genai==1.75.0
fastapi==0.136.1
uvicorn[standard]==0.47.0
httpx==0.28.1
firebase-admin==7.4.0
pydantic==2.12.5
python-dotenv==1.1.1
sentry-sdk[fastapi]==2.30.0

# Phase 2: Cloud Trace via OpenTelemetry. ADK emits spans natively; these two
# packages provide the OTLP/HTTP exporter and Google Cloud resource detector
# that the ADK `telemetry.google_cloud.get_gcp_exporters` helper depends on.
opentelemetry-exporter-otlp-proto-http==1.41.1
opentelemetry-resourcedetector-gcp==1.12.0a0

# Phase 3: Vertex AI Search grounding. Backs the `lookup_drill_knowledge`
# tool against the curated drill corpus in `knowledge/`. Lazy-imported in
# `app/tools/grounding.py` so the dependency only costs when grounding is
# actually enabled at runtime.
google-cloud-discoveryengine==0.19.0
