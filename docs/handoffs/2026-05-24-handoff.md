# Buddy Live · 2026-05-24 handoff

> **Historical journal — not current spec.** For live behavior see [README.md](../../README.md) and [ARCHITECTURE.md](../ARCHITECTURE.md).

End of day journal. Picks up where the [audit](../audits/2026-05-23-audit.md) left off.

> **Update 2026-05-25 (pm):** Conversation UI plan **complete** except deferred interrupt. Frontend deployed to Vercel (`ee8e1b1` → production). Prompt personality polish in `4a78321`. **No local testing** — verify on Vercel only; see [Testing](#testing-where-to-run).
>
> **Update 2026-05-25 (eve):** Live session tuning — voice auto-reconnect, timed warm-up (`start_warmup_timer` + `peek_warmup`), voice-led setup framing, spoken demos for ages ≤10. See section **F. Live session tuning** below.
>
> **Update 2026-05-26/27:** Visual-cue hardening + cleanup pass + observability. New sections **G–K** below cover: multi-frame warm-up vision, framing banner that no longer blocks the drill chip, drill-id canonicalisation, hidden-message centralisation, transcript scroll UX, recording auto-stop pulse, Sentry on the ADK service, durable per-session summary doc for weekly review, GCS+Firestore TTL-based daily cleanup, dynamic session phases on cards, robust ElevenLabs reconnect handlers, child-friendly IQ practice modes, and browser socket saturation protection.
>
> **Update 2026-05-27 (eve):** Reconnection-loop hardening, suppression of duplicate greetings on websocket reconnect, and camera/warmup vision-check simplification (verbal-only confirmation). Added sections **L–M** below.
>
> **Update 2026-05-28 (eve):** First E2E E2E-session feedback loop improvements — migrated ElevenLabs voice to WebRTC for drop-free silence keep-alives, added active cooldown tasks during analysis wait state, refined 1-ball rep count, added visually explicit recording "stop" instructions, redesigned RepScorecard with beautiful stats/progress bars, and implemented a full-center recap dashboard with a floating Picture-in-Picture camera view. Added section **N** below.
>
> **Update 2026-05-29 (eve):** Session triage round 2 (`live-cdp0na318hs6`, `live-xzt3prnl5837`, `live-07h5df7rann7`) + a Sentry alert. Shipped: prefix-aware dedupe + user-text trimming, coach-output sanitisation, IQ phase set on hand-off, IQ card no longer advances mid-follow-up, age-varied scenario order, teammate-in-slot diagram fix, warm-up message no longer contradicts the prompt, **rep-clip upload moved direct-to-Storage to fix Vercel 413s**, ElevenLabs `background_voice_detection` enabled, and **in-process Gemini retries for transient 503s**. Silence disconnects diagnosed as systemic/pre-existing (not a regression) — root cause still needs the browser-console drop reason. Added section **O** below.
>
> **Update 2026-05-29 (late eve):** Session triage round 3 (`live-oxgcwzvwj2x9`) + single-rep flow lock-in. **Root cause for several reliability bugs found: the ADK backend keeps conversation history in `InMemorySessionService`, but Cloud Run was scaling to 5 instances** — reconnects routed to a cold instance lost all history, so the coach cold-restarted ("What's your name?"), couldn't see the rep scorecard (re-recorded instead of reviewing), and bypassed in-memory turn dedupe (duplicate goodbye). **Pinned to one warm instance** (`min=max=1`, concurrency 20), applied live (rev `00045-b4g`) + in `infra/cloudbuild.yaml`. Also: assume **one** scored video per session (prompt + UI + structural `phase_guard`), agent now announces "your results are ready — ready to review?" with the scorecard **full-screen in the center** (no "Session Completed" chrome), IQ scenarios ask the choice once (no "What is your play?" double-question + missing space), `_trim_user_text` no longer clips the `(Voice reconnected …` system note, reconnect note carries the last rep id + awaiting-review flag, ElevenLabs cold greeting suppressed on resume via `overrides.agent.firstMessage`, and vision tools are blocked after recap/ended (no post-session peek loop). Added section **P** below.
>
> **Update 2026-05-30:** Track 2 Phase 5 multi-agent splits (`vision_coach`, `drill_coach`) + GEPA harness fix. Living phase journal with eval baselines: [`TRACK2-PHASE-JOURNAL.md`](../TRACK2-PHASE-JOURNAL.md). Added section **Q** below.
>
> **Update 2026-05-30 (cleanup):** Legacy code/doc pass — removed unused UI state, deprecated `CameraView.recording`, orphan `schema.d.ts` + `make gen-types`, Next.js boilerplate SVGs, duplicate `marcus_returning_scenario.json`, and a 1.1 MB tracked eval artifact. Centralised drill-id mapping in `lib/drills.ts`. Docs synced to **single-rep flow**, **15 tools**, verbal setup default; historical audits/handoffs moved under `docs/`. Added section **R** below.
>
> **Update 2026-05-30 (UI polish):** Landing + `/coach` share one design system (`globals.css` brand tokens, vignette background, panel surfaces). Landing preview now mocks **both** practice paths — scored-rep scorecard + Hockey IQ diagram — so visitors see what they get before mic/camera. Added section **S** below.

## What landed today

### 1. Repository → GitHub + Vercel + Cloud Run pipeline
- Pushed the monorepo to `jakedibattista/buddy-live`.
- Vercel wired up with **Root Directory = `apps/buddy-live`**; auto-deploys frontend on push to `main`.
- GitHub Actions workflow `.github/workflows/deploy-backend.yml` auto-deploys the ADK service to Cloud Run on push to `main`.
- GCP IAM straightened out (long debugging chain — see commit history):
  - `github-deployer` SA: `roles/serviceusage.serviceUsageConsumer`, `roles/storage.admin`
  - Cloud Build worker SA: `roles/iam.serviceAccountUser` on the runtime SA
  - `buddy-live-adk` runtime SA: `roles/datastore.user`, `roles/storage.objectViewer`, `roles/secretmanager.secretAccessor`
- Firestore rules merged surgically (`infra/firestore.rules.merged`) — kept all of puck-buddy's existing rules intact and added the `live_sessions` block.

### 2. Drill selection moved into the voice flow
- Landing page collapsed to a single emerald **"Talk to Coach Buddy"** CTA.
- Coach Buddy now asks "wristshot, slapshot, or backhand?" in the opening turns.
- New ADK tool **`set_focus_drill(drill_id)`** persists the choice to `live_sessions/{sid}.focus_drill`.
- `DrillChip` on the camera view subscribes to that Firestore field and lights up before the first rep.
- Stripped `?drill=` query-param plumbing from `/coach`, `/api/session`, `useLiveSession`.
- Removed the scripted demo route (`/coach/demo`) and its fixture data.

### 3. Coach Buddy system prompt rewrite
- Lives at `services/buddy-live-adk/app/prompts.py` (deployed via Cloud Run).
- **2026-05-25 flow:** greeting → name → age → drill pick → `set_focus_drill` → **timed warm-up** (4 moves, 20–30s each, on-screen timer, verbal feedback) → verbal setup check → **drill readiness** (explain or practice rep) → **one scored rep** (IQ questions while analyzing) → results-driven recap + homework.
- No more `{focus_drill?}` placeholder; agent fully owns the flow.

### 4. ElevenLabs agent (`agent_4501ksad2mf6fw5vdqqzzjcp9n3b`) settled config
Took a long debug chain to land here — DO NOT EDIT THESE IN THE DASHBOARD without re-verifying. Both API-set:

| Field | Value | Why |
|---|---|---|
| `conversation_config.agent.prompt.llm` | `custom-llm` | |
| `conversation_config.agent.prompt.custom_llm.url` | `https://buddy-live-adk-7iv2gspkgq-uc.a.run.app` *(base — ElevenLabs auto-appends `/chat/completions`)* | |
| `conversation_config.agent.prompt.custom_llm.model_id` | `gemini-flash-latest` | |
| `conversation_config.agent.first_message` | `Hey, I'm Coach Buddy. What's your name?` | Spoken instantly via TTS on connect, no LLM round-trip |
| `platform_settings.overrides.custom_llm_extra_body` | `true` | Lets client pass `arbitrary_identifier: sessionId` so ADK maps the conversation to the right Firestore doc |
| TTS voice | `yjJ45q8TVCrtMhEKurxY` / `eleven_v3_conversational` / 0.5 / 0.8 / 1.0 | |

ElevenLabs dashboard's "remove `/chat/completions` from URL" validation is correct — they auto-append it at runtime.

### Proof of life — `conv_9401kse0yrwse938jhwb2hg3qe34` (113s, 15 msgs)

```
[agent] "Hey, I'm Coach Buddy. What's your name?"
[user]  Jake.
[agent] Awesome to meet you, Jake! How old are you?
[user]  Twenty-five.
[agent] Awesome. What do you want to work on today -- wristshot, slapshot, or backhand?
[user]  Backhand.
[agent] Got it, backhands are locked in. I don't see you yet, Jake. Step back into the frame...
[user]  Can you see me now?
[agent] Still can't see you, Jake. Make sure you're stepped back a few feet...
[user]  How about now?
[agent] I still can't see you, but let's go anyway! Start the puck on your back heel, sweep through, and roll those wrists. Ready?
[user]  Stop.
[agent] No problem, Jake. What's up?
[user]  Can you ask me a hockey IQ question?
[agent] Sure! If you're entering the zone on a 2-on-1, should you look to pass or shoot first?
```

---

## Completed 2026-05-25 (was “open items for tomorrow”)

### A. peek_camera blind spot — **DONE** (`e08cf9b`, `ee8e1b1`)
- Root cause: vision model conflated “no stick / upper body only” with invisible; not a pipeline failure.
- Fix: structured PERSON/STICK/SETUP/COACH prompt; `person_visible`, `full_body_in_frame`, `facing_camera`, `setup_framing_passed`; never say “I don’t see you” when `person_visible=true`; amber camera-hint banner after 2 failed peeks; peek uploads start when session + camera exist.

### B. Conversation design — **DONE** (`db98331`, `4a78321`)
- Hockey tangents: one sentence + redirect (IQ questions are **in-flow** during analysis wait, not tangents).
- Visibility: no “let’s go anyway” while framing fails; UI banner + `camera_hint` on session doc.
- Graceful end: red phone button → wrap-up message → ~12s → disconnect; `end_session_recap` gated on ready scorecard.
- Prompt personality: phase transition bridges, rep counting aloud, varied coaching voice (see §E).

### C. Recording flow — **DONE** (`6210065`, `ee8e1b1`; E2E verified in live session)
- `stop_rep_capture` → client stops + uploads; 60s REC countdown + **Stop & upload**; `/api/reps/analyze` idempotent kickoff; `analyze_rep` polls for clip; `useRepResultsPolling` refreshes jobs; scorecards in side panel; `results_ready_at` on session when first rep completes.

### D. Conversation UI (Lovable chatbot UX plan) — **DONE** (`ee8e1b1`)
- Phase 1: system transcript pills, activity indicator, setup framing gate UI, error recovery, 60s REC + Stop & upload.
- Phase 2: `CoachPuckAvatar` — volume-reactive mouth, dim while recording, celebrate on score.
- Phase 3: timestamps, long-message split, phase label, voice quick prompts, coach output mute.
- Assets: `public/mascot/coach-puck.png` + `coach-puck-speak.png` — face baked into PNG (charcoal eyes, smirk / open mouth); no SVG overlay.
- Plan doc: [UI-CONVERSATION-UX-PLAN.md](../UI-CONVERSATION-UX-PLAN.md).

### E. Agent prompt personality — **DONE** (`4a78321`)
- Added `PERSONALITY`, `PHASE TRANSITIONS`, and coaching-voice guidance in `prompts.py`.
- Bridge lines between warm-up → setup → drill readiness → reps → recap.
- Rep counting spoken aloud (target 5); IQ questions framed as “what would you do?” not quiz mode.

### F. Live session tuning — **DONE (2026-05-25 eve, deploy pending)**

Root causes from live testing: ElevenLabs voice drop felt like a full restart; warm-up used jargon and no movement feedback; setup framing relied on UI overlay instead of coach voice.

| Area | What shipped |
|------|----------------|
| **Voice reconnect** | `CoachConversation` auto-reconnect (5 attempts, backoff, `useWakeLock`); resume first message + ADK context message; `lib/voiceResume.ts` |
| **Warm-up timers** | `start_warmup_timer` ADK tool → Firestore command → `WarmupTimerBridge` + shared `CountdownOverlay` (m:ss, amber) |
| **Warm-up feedback** | `peek_warmup` after each timer; sidebar cues + system transcript |
| **Plain language** | Prompt `SPOKEN DEMO` lines for ages ≤10; never label-only (“stick wipers” only on timer after demo) |
| **Setup framing** | Coach speaks feet/framing fixes; `peek_camera` skipped during `warmup` phase |

**Warm-up move durations:** arm circles 20s, high knees 30s, stick wipers 20s, shadow shot 30s (drill-specific).

**Files:** `CoachConversation.tsx`, `CountdownOverlay.tsx`, `WarmupTimerBridge.tsx`, `useWarmupTimer.ts`, `warmup_timer.py`, `peek_warmup.py`, `prompts.py`, `types.ts` (`parseCoachCommand`).

### Commits (2026-05-25)
| Commit | Summary |
|---|---|
| `e08cf9b` | peek_camera structured vision + framing gate |
| `db98331` | Tangents, camera hints, graceful end |
| `6210065` | stop_rep_capture, 60s recording, analyze race fix |
| `ee8e1b1` | Conversation UI phases 1–3 (mascot, transcript, phases) |
| `4a78321` | Prompt personality + doc sync |
| `6a6dfbf` | Mascot v2 — baked face PNGs, remove SVG overlay |

---

## What landed 2026-05-26/27

### G. Visual cue hardening + conversational pacing — **DONE** (`43c4186`)

Root causes from a live test: framing banner persisted with feet/head in frame and obscured the drill chip; warm-up advanced without checking that the kid actually moved.

| Area | What shipped |
|---|---|
| **Banner positioning** | Moved framing banner from `inset-0 z-20` to a top-left card (`max-w-[55%]`, `z-10`); drill chip elevated to `z-30` so it stays visible |
| **Stale-banner nudge** | New `CameraPeekNudge` silently sends a `(Camera check)` hidden message to the agent if framing hasn't refreshed for > 10s; rate-limited to 12s, logged to the system transcript |
| **Soft frame indicator** | `FramingIndicator` shows an amber pill if the player drifts out of frame **after** the initial setup gate already passed (no banner takeover) |
| **Multi-frame warm-up vision** | `/api/peek` now writes a `peek_url_history` ring buffer (PEEK_HISTORY_MAX=8); `usePeekFrameUploader` drops to 1.5s cadence while a warm-up timer is active |
| **Strict motion gating** | `peek_warmup` rewritten to pull up to 4 spaced frames and parse a strict `MOTION=yes/no/unclear` line; `last_warmup_motion_detected` mirrors that verdict |
| **Prompt** | Warm-up loop in `prompts.py` now (a) asks if the kid knows the move and demos if not, (b) waits for explicit "ready" before `start_warmup_timer`, (c) hard-gates progression on `moving=false`. Opening conversation has explicit one-question-per-turn pacing after name/age. |

### H. Cleanup pass — **DONE** (`4e8666e`)

| Area | What shipped |
|---|---|
| **Hidden messages** | All agent-side hidden messages (voice reconnect, warm-up timer done, camera re-check) consolidated into `apps/buddy-live/src/lib/hiddenAgentMessages.ts` with a shared `isHiddenAgentMessage` filter for the transcript |
| **Drill ids canonicalised** | `DrillId` = `wristshot | slapshot_form | backhand`; legacy `snapshot` / `skating` removed from `types.ts`, `DrillChip`, `rep_capture.py`, `coaching.py`. `SessionPhase` consolidated to `scored_reps`. |
| **Recording timer pulse** | `CountdownOverlay` amber warning ≤5s, hard red pulse ≤3s for both REC + warm-up variants |
| **Transcript scroll UX** | `TranscriptPanel` pauses auto-scroll when the user scrolls up; "New messages" jump-to-latest button appears when content arrives off-screen |
| **Token budget** | `max_output_tokens` on both peek tools dropped 512 → 128 (their formats only need ~80 tokens) |

### I. Production hardening — **DONE** (`a1c897a`, `e018653`, `45fb3c5`)

| Area | What shipped |
|---|---|
| **Parser tests** | `services/buddy-live-adk/tests/test_parsers.py` covers `peek_camera`, `peek_warmup`, and `rep_capture` drill normalisation (16 cases). `make test` runs them via pytest. |
| **Sentry on ADK** | `sentry-sdk[fastapi]==2.30.0` wired in `app/main.py`, gated on `SENTRY_DSN` env. Project `buddy-live-adk` configured on Cloud Run; revision `buddy-live-adk-00020-b6f` logs `sentry initialized environment=production`. (First attempt failed because `enable_logs=True` requires SDK ≥2.35.0; fixed by dropping that single arg.) |
| **Session learning summary** | New top-level `session_summaries/{sessionId}` collection written by `end_session_recap`. One compact row per session with `drill`, `rep_count`, `weakest_metric`, `framing_struggles`, `warmup_motion_misses`, `final_phase`, etc. Lives outside `live_sessions/` so the TTL does NOT delete it — this is the Sunday review record. |
| **Per-session counters** | `peek_camera` increments `framing_failure_count` on pass→fail transitions; `peek_warmup` increments `warmup_motion_miss_count` per no-motion verdict. Both surface in the summary doc. |
| **Storage cleanup** | GCS Object Lifecycle rule deletes `gs://puck-buddy.firebasestorage.app/live_sessions/**` after 1 day (`infra/storage-lifecycle.json`). Firestore TTL policy ACTIVE on `live_sessions.expires_at`; web `/api/session` now stamps `expires_at = now + 24h` on session create. |
| **Weekly review doc** | `infra/storage-lifecycle.md` documents the schema, where to look (Firebase console → `session_summaries`), triage questions, and the iterate-the-prompt loop. |

### Commits (2026-05-26/27)
| Commit | Summary |
|---|---|
| `1b8bb0a` | Live session resilience + timed warm-up coaching |
| `43c4186` | Harden visual cues + warm-up coaching flow |
| `4e8666e` | Cleanup pass: hidden-message module, legacy drill ids, framing indicator, recording pulse |
| `a1c897a` | Production hardening: ring buffer 8, transcript pause, parser tests, Sentry, lower peek tokens |
| `e018653` | Drop `enable_logs` Sentry option incompatible with sentry-sdk 2.30 |
| `45fb3c5` | Per-session learning summary + 24h storage cleanup |

---

## What landed 2026-05-27 (pm)

### J. Agent runaway fix + session resilience — **DONE**

Root cause from a live session (`live-pgi5ponazebf`): during the second warm-up action (arm circles retry), the agent entered a degenerate tool-calling loop that ran for 301 seconds, produced a 504KB SSE response, and was killed by Cloud Run's timeout. Contributing factors: stale peek frames, "..." silence fillers polluting the conversation, overlapping concurrent turns from ElevenLabs, and no guard on the ADK runner's LLM call count (default was 500).

| Area | What shipped |
|------|----------------|
| **max_llm_calls cap** | `RunConfig(max_llm_calls=10)` — caps tool-calling rounds per user turn. Normal ops use 2–5; prevents runaway loops. |
| **Silence filter** | `"..."`, `""`, `"."`, `".."`, `"…"` from ElevenLabs silence detection are returned as empty SSE immediately — no Gemini call, no session history pollution. |
| **Per-session lock** | `asyncio.Lock` per session_id serializes concurrent turns. Two utterances arriving 200ms apart no longer corrupt InMemorySessionService state. |
| **30s turn timeout** | `asyncio.timeout(30)` around `run_async`. If a turn hangs, bails with "Hold on one sec — let me catch up." instead of burning 5 minutes. |
| **Clear peek_url_history on timer start** | `start_warmup_timer` resets `peek_url_history: []` so `peek_warmup` only analyzes frames captured during the exercise, not stale pre-timer frames. |
| **Flexible rep count** | Prompt now asks how many pucks/balls the player has. Supports 1-puck workflows (shoot → pick up → shoot again). Player can bail after any rep with results. |

**Files:** `app/main.py`, `app/tools/warmup_timer.py`, `app/prompts.py`

---

### K. IQ Mode polish + Reconnect race fix + Client-side connection saturation fix — **DONE** (`8f652ea`, `726f374`, `f72bc12`)

Root causes from live tests (`live-100087zdo0yq`, `live-2zzlfdgpnumi`, `live-ir01l86875m0`): (a) prompt asked for drill before checking space/equipment; (b) visual scenario cards popped up before questions were asked; (c) visuals were constrained to a tiny sidebar; (d) right/wrong options lacked feedback; (e) language was too complex; (f) voice shifted entering IQ mode; (g) reconnection had a race condition causing infinite loops; (h) uploader flooded connections, causing WebRTC drops; (i) cards showed stale hardcoded '5 reps' text.

| Area | What shipped |
|------|--------------|
| **Reordered opening** | Space, stick, and puck check now comes **before** drill selection. If the player lacks space, they hand off to the IQ sub-agent immediately without locking or showing shooting drills. |
| **Big IQ overlay** | During `currentPhase === "iq_practice"`, we completely hide the camera and place a beautiful, full-size overlay card with a much larger half-rink visual diagram. All camera, framing, and timer overlays are hidden to prevent clutter. |
| **Visual timing** | Prompts are hard-gated to say the scenario out loud first, then call `show_iq_visual` at the very end of the turn so the card lands as reference instead of pre-empting speech. |
| **Answer marking** | New tool `mark_iq_answer(player_choice, correct_choice)` writes to Firestore. The card animates correct choices in green with a ✓, and wrong choices in red with a ✕. Auto-clears when the next scenario fires. |
| **Child friendly prompt** | Explicit child-friendly (ages 10–11) vocabulary level. No un-explained jargon (five-hole, slot, etc.). Includes a dynamic "Rules Mode" with rules-only scenarios if they say they want to learn the rules of the game. |
| **Voice continuity** | `IQ_COACH_PROMPT` now mirrors the main coach's style and personality blocks and forces contractions ("let's", "you're") so the voice is 1:1 identical across handoffs. |
| **Reconnect race fix** | Eliminated client-side race condition in `CoachConversation.tsx` where resume messages were sent on a raw 900ms timer immediately after `startSession()`. Resume triggers are now queued and sent safely 500ms after ElevenLabs invokes `onConnect()`. |
| **Socket saturation** | Introduced an `inFlightRef` guard in `usePeekFrameUploader.ts` to prevent parallel uploads. Chrome's 6-connection limit was previously saturated with slow base64 uploads, blocking WebRTC websocket pings and Firestore snapshot listeners. |
| **Dynamic card phases** | Cleaned up the distracting hardcoded "5 reps" overlay. The DrillChip top-bar now dynamically updates to show exactly what phase the user is in: "Warm-up", "[Drill] practice" (e.g. Slapshot practice), "Cooldown & review", or "Hockey IQ". |

### L. Voice Reconnection on Silence Disconnect — **DONE** (`800fd6e`)

Root causes from live tests (`live-ajd84d3lvy2m`): (a) ElevenLabs closed websocket connections after a period of silence (e.g. bathroom break) due to inactivity timeouts; (b) subsequent client-side reconnection attempts failed instantly because of unauthorized `firstMessage` overrides passed in `startSession` settings; (c) without the override, the agent would repeat the default welcome/greeting on reconnect; (d) a premature reset of the `reconnectAttemptRef` inside `connectSession` and lack of defensive `details?.reason` checks caused an infinite "(1/5)" reconnect notification loop.

| Area | What shipped |
|------|--------------|
| **Remove Overrides** | Removed unauthorized `overrides` entirely from `sessionOptions` in `CoachConversation.tsx` so reconnect requests are never rejected by ElevenLabs. |
| **Reconnect Detection** | Added session existence checks in `services/buddy-live-adk/app/main.py`. The ADK backend now suppresses the welcome greeting on reconnect by sending a silent `"(voice connection restored - wait for user to speak)"` system prompt instead of `"(session start)"`. |
| **State Resume** | Seamlessly resume the coach session exactly where it left off, based on client-side state transmitted 500ms after reconnection. |
| **Reconnect Loop Fix** | Fixed `CoachConversation.tsx` by removing the premature counter reset on start and adding defensive checks for `details?.reason` in `onDisconnect` to strictly enforce the 5-attempt retry limit and prevent UI looping. |
| **English-only Polish** | Added strict English-only guidelines to `COACH_SETH_LIVE_PROMPT` and `IQ_COACH_PROMPT` in `app/prompts.py` to prevent hallucinating foreign phrases like *"Tolong dijawab"*. |

### M. Camera & Warm-up Vision Simplification (Conversational Pacing) — **DONE**

Root causes from user feedback: Automated vision checking (`peek_camera` and `peek_warmup`) added unnecessary latency, friction, and potential false negatives during the warm-up and stance check phases. The user requested a conversational verbal confirmation instead.

| Area | What shipped |
|------|--------------|
| **Automatic framing bypass** | Modified `set_focus_drill.py` to automatically write `"setup_framing_passed": true` to the session Firestore document as soon as the drill is set, preventing any downstream blockers. |
| **Conversational warmup flow** | Rewrote section 2 (Warm-up) in `prompts.py` to instruct Coach Buddy **not** to call `peek_warmup` when the warmup timers finish. The coach now asks verbally how they felt, explains the next move, and transitions directly. |
| **Conversational setup check** | Rewrote section 3 (Setup check) in `prompts.py` to instruct Coach Buddy **not** to call `peek_camera` during setup. The coach now simply asks for verbal confirmation that they are wholly in frame (head to toes), have their stick and puck/ball, and are ready. |
| **Camera reserved for scored reps** | Both the prompt and tool calling rules preserve the video recording and upload pipeline (`start_rep_capture`, `stop_rep_capture`, `analyze_rep`, `get_rep_result`) exclusively for active shooting drills. |

### N. E2E Feedback Loop & Session Performance Polish — **DONE** (2026-05-28)

Root causes from first end-to-end user session: (a) WebSocket silence disconnects are highly disruptive; (b) players were left standing confused during video analysis with no physical recovery task; (c) single-ball players had multiple reps or bonus reps queued incorrectly; (d) recording stop cues lacked clarity; (e) the scorecard UI was hard to read and lacked visual indicators; (f) players wanted the results recap displayed directly in the center panel where their camera was, while still being able to see their face.

| Area | What shipped |
|------|--------------|
| **WebRTC Transition** | Updated `signed-url` route to fetch WebRTC tokens alongside WebSocket pre-signed URLs. Integrated WebRTC session initiation via `conversationToken` inside `CoachConversation.tsx` for production-grade, silence-resilient, low-latency audio stream keep-alives. |
| **Active Cooldown / Recovery** | Updated `prompts.py` to instruct Coach Buddy to assign a simple physical cooldown/active recovery task (e.g., easy stickhandling, shoulder rolls, forearm stretches) during the 30–90s video analysis wait window. |
| **1-Puck Pacing** | Updated `prompts.py` so if the player has only 1 ball/puck, the coach sets the target to exactly 1 scored rep and **completely suppresses** the "bonus rep" queue during analysis processing, remaining conversational instead. |
| **Recording "Stop" Guidance** | Added explicit instructions on the NextTurnCue banner (*`Perform your rep — say "stop" or hit the red button when done`*) and directly on the CountdownOverlay recording overlay (*`Say "stop" or hit the button when done`*). |
| **Scorecard Redesign** | Fully overhauled `RepScorecard.tsx` to include an average score pill, horizontal visual progress bars (0-10) color-coded by performance thresholds, and highlighted "Strongest cue" and "Focus area" callout cards using Lucide icons. |
| **Full-Center Recap Dashboard** | Overhauled `page.tsx` during recap/ended phases. It now displays the detailed performance reports and scorecards directly in the main center panel. |
| **Floating Camera PiP** | Integrated a Picture-in-Picture camera system. When entering recap or ended phases, the camera smoothly shrinks and floats in the bottom-right corner rather than unmounting or disappearing. |
| **Disconnection Integrity** | Enforced that if `userEndedRef` or a final phase is active, no reconnection loops can spawn, cleanly wrapping up on the final turn or when the red phone button is pressed. |

### Commits (2026-05-27 pm)
| Commit | Summary |
|---|---|
| `8f652ea` | fix(coach,adk): IQ flow polish — space check first, big overlay, answer feedback, kid-friendly voice |
| `726f374` | fix(ui): eliminate ElevenLabs reconnect loop and remove hardcoded '5 reps' overlay |
| `f72bc12` | fix(ui): prevent camera uploader browser connection saturation and show dynamic phase names on DrillChip |
| `800fd6e` | fix(voice): resolve reconnection failures and double greetings on silence disconnect |

---

## What landed 2026-05-29 (eve)

### O. Session triage round 2 — IQ pacing, clip-upload 413, Gemini retries — **DONE**

Root causes from live tests (`live-07h5df7rann7`, `live-xzt3prnl5837`, `live-cdp0na318hs6`) and a Sentry alert: (a) long open-mic transcripts from ElevenLabs ballooned latency and the agent re-ran near-duplicate utterances; (b) internal model reasoning / speaker labels ("Stafford:", "(thought) …") leaked into spoken output; (c) the UI phase chip stayed on "Warm-up" after hand-off to IQ; (d) the IQ card advanced to the next question while the player was still on a follow-up; (e) scenarios always opened with the breakaway in the same order regardless of age; (f) a teammate "in the slot" rendered in a top corner; (g) the warm-up timer-done message told the agent to call `peek_warmup`, contradicting the prompt; (h) rep-clip upload returned **HTTP 413**; (i) Sentry showed Gemini **503 UNAVAILABLE** surfacing as failed turns; (j) voice still drops during silence.

| Area | What shipped |
|------|--------------|
| **Prefix-aware dedupe** | `app/main.py` collapses bursts where a new utterance is a prefix/superset of a just-answered one within a short window — kills the "accumulation storm" from open-mic transcripts. |
| **Trim user text** | User text fed to the agent is capped (~240 chars, sentence-aware) so a rambling open mic can't balloon turn latency. |
| **Coach output sanitisation** | Coach text is cleaned of speaker labels and chain-of-thought parentheticals before TTS (`_clean_coach_text`). Covered by `tests/test_turn_handling.py`. |
| **IQ phase on hand-off** | When the `iq_coach` sub-agent first responds, `currentPhase` is set to `iq_practice` immediately so the UI chip flips off "Warm-up" without waiting for the first `show_iq_visual`. |
| **Intro phase** | New `intro` phase (label "Intro") is the session default, so the opening conversation (name/age/space/drill pick) no longer mislabels as "Warm-up". It flips to `warmup` only when the player commits to shooting (`set_focus_drill` / `start_warmup_timer`), or to `iq_practice` on IQ hand-off. |
| **Warm-up 3-2-1 count-in** | `useWarmupTimer` now runs a 3s lead-in (`phase: "leadin"`) showing a big 3 → 2 → 1 before the exercise timer, so "Go!" no longer lands on a static clock. `CountdownOverlay` renders the count-in; prompt cue updated to "counting you in on screen... three, two, one!". |
| **No premature card advance** | `IQ_COACH_PROMPT` now requires a check-in beat ("Make sense? Ready for the next one?") and an explicit player go-ahead before the next `show_iq_visual`. The on-screen card must not change mid-discussion. |
| **Varied, age-scaled order** | Prompt instructs the agent to vary the opening scenario / sequence each session and scale starting difficulty to age (≤10 rules-first, 11–13 shot-selection, 14+ multi-read) instead of always opening with the breakaway. (LLM determinism limits perfect randomness.) |
| **Teammate diagram fix** | `parseDiagramPositions` now places a teammate described "in the slot" / "in front" / at a circle / point in the correct spot instead of the opposite-wing top corner. Removed the "Coach's Whiteboard" label; question header restyled to the neutral section-label style. |
| **Warm-up message coherence** | `buildWarmupTimerDoneMessage` no longer tells the agent to call `peek_warmup` (the prompt forbids it). It asks for verbal feedback + the next move. |
| **Clip upload 413 fix** | Rep clips exceeded Vercel's hard **4.5 MB serverless body limit**. The browser now mints a signed PUT URL (`/api/clips/upload-url`), uploads the video **directly to Firebase Storage** (bucket already allows cross-origin PUT), then finalises via `/api/clips/upload` with a small JSON body. Video bytes never traverse a Vercel function. |
| **ElevenLabs VAD** | `conversation_config.vad.background_voice_detection` set to `true` on the live agent so bystander chatter / drill noise is less likely to open the player's turn. (REST PATCH; not in the SDK or MCP.) |
| **Gemini transient retries** | Both agents use a `Gemini` model with `HttpRetryOptions(attempts=3, codes=[429,500,502,503,504], 1s→4s backoff)` so transient 503s retry in-process (incl. mid-stream) instead of failing the turn. |

**Silence disconnects (still open):** ElevenLabs conversation records show each session fragmenting into many short `failed` conversations — every reconnect spawns a new one. This is **pre-existing** (the streaming-backend session `live-07h5df7rann7` had 9/10 conversations fail too), so it is not a regression from the buffering change. The actual WebRTC drop reason only surfaces in the **browser console** (`[voice] unexpected disconnect …`, logged in `CoachConversation.tsx`). **Next step:** capture that `details.reason` during a session to choose the precise fix (custom-LLM turn timeout vs. ElevenLabs inactivity vs. network).

**Files:** `app/main.py`, `app/prompts.py`, `app/agent.py`, `tests/test_turn_handling.py`, `apps/buddy-live/src/components/IqVisualCard.tsx`, `apps/buddy-live/src/lib/hiddenAgentMessages.ts`, `apps/buddy-live/src/hooks/useRepCapture.ts`, `apps/buddy-live/src/app/api/clips/upload/route.ts`, `apps/buddy-live/src/app/api/clips/upload-url/route.ts`.

### P. Session triage round 3 — single-instance fix, single-rep flow, reconnect resilience — **DONE**

Source: full Firestore read of `live-oxgcwzvwj2x9` ("Bob", 50, slapshot) — every turn, command, rep, and the session doc. The session had ~3–4 reconnects in 7 min; after one drop the coach said *"Hey! I'm Coach Buddy. What's your name?"*, then *"I don't have that first scorecard handy — let's fire another slapshot"* (it re-recorded), and the goodbye fired twice.

**The unifying root cause:** `app/agent.py` uses `InMemorySessionService` (conversation history in process memory) while Cloud Run ran `maxScale=5`. Turns/reconnects landing on a different instance had **no session history**, which independently produced the cold re-greet, the lost scorecard, and the cross-instance dedupe miss.

| # | Symptom (evidence) | Fix |
|---|---|---|
| 1 | Cold re-greet on reconnect (23:22:09 "What's your name?") | **Pin backend to 1 instance** (`infra/cloudbuild.yaml` `_MAX_INSTANCES=1` + live `gcloud run services update`, rev `00045-b4g`) so history survives reconnects; suppress ElevenLabs' canned greeting on resume via `overrides.agent.firstMessage` (`CoachConversation.tsx`); prompt VOICE RECONNECT rule: never re-greet. |
| 2 | Reconnect re-records instead of reviewing (23:22:38) | Reconnect note now carries `lastRepId` + `awaitingReview` (`hiddenAgentMessages.ts`, `coach/page.tsx`); prompt: if a rep awaits review, call `get_rep_result` and review, never `start_rep_capture`; **structural backstop** — `phase_guard` blocks `start_rep_capture` once any rep is `completed` (single-rep policy). |
| 3 | `_trim_user_text` clipped the `(Voice reconnected …` prefix (stored note began mid-sentence) | `_trim_user_text` now returns parenthetical/system messages untrimmed, so the reconnect cue survives and the prompt's "starts with Voice reconnected" rule fires. |
| 4 | IQ "double question" + missing space (*"…keep them in?What is your play?"*) | `IQ_COACH_PROMPT` step 1: ask the choice **exactly once**, never append a second question. |
| 5 | Disconnects during silence (still ~3–4/session) | Now **state-preserving + seamless** (single instance keeps history; resume first-message override hides the reconnect). Transport-drop root cause still needs the browser-console `details.reason` (logged in `CoachConversation.tsx onDisconnect`). |
| 6 | Duplicate goodbye (identical wrap-up 2s apart, 23:22:52 & :54) | Fixed by the single-instance pin — in-memory dedupe + `asyncio` locks are now authoritative. |
| 7 | 8 `peek_camera` fires ~8 min **after** session ended | `phase_guard` blocks `peek_camera`/`peek_warmup` when `currentPhase ∈ {recap, ended}` or `ended_at` is set. |

**Single-rep flow (assume one video):** prompt §4/§5/§6 collapsed to one scored rep (no "how many pucks?", no bonus reps); on results-ready the coach says *"Your results are ready — ready to review?"* and walks the scorecard that is now **full-screen in the center**. `SCORED_REP_TARGET=1`; side-panel rep list removed; `NextTurnCue`/hint copy de-numbered. UI: single rep renders one `RepScorecard` at `max-w-3xl` with **no "🏆 Session Completed" banner** — the card header (`Slapshot Form` / `ID: …`) is the heading. Report now appears the moment `results_ready_at` lands (not only at recap).

**Trade-off / next step:** one instance is correct for current single-user load, but if that instance restarts mid-session, history is still lost (the reconnect note + `get_rep_result` recovery path covers it). The durable fix is a persistent `SessionService` (Vertex AI / DB-backed) — logged for later. The ElevenLabs `first_message` override only takes effect if **overrides are enabled** on the agent (see "ElevenLabs first-message override" below).

**Files:** `infra/cloudbuild.yaml`, `app/main.py`, `app/callbacks.py`, `app/prompts.py`, `apps/buddy-live/src/lib/recording.ts`, `apps/buddy-live/src/lib/hiddenAgentMessages.ts`, `apps/buddy-live/src/components/CoachConversation.tsx`, `apps/buddy-live/src/components/NextTurnCue.tsx`, `apps/buddy-live/src/app/coach/page.tsx`.

#### ElevenLabs first-message override (needs dashboard or REST — not in MCP/SDK)
The MCP `get_agent` returns only name/voice/id (no `platform_settings.overrides`), and there is **no `update_agent` MCP tool**, so this can't be verified or set via MCP. To enable the resume first-message override (agent `agent_4501ksad2mf6fw5vdqqzzjcp9n3b`):

- **Dashboard:** Agent → **Security** (a.k.a. Advanced/Overrides) → enable the **First message** override toggle.
- **REST (run locally where `ELEVENLABS_API_KEY` lives — do NOT prefix the value with `$` and do NOT paste it in chat):**

```bash
# Inspect current overrides
curl -sS "https://api.elevenlabs.io/v1/convai/agents/agent_4501ksad2mf6fw5vdqqzzjcp9n3b" \
  -H "xi-api-key: $ELEVENLABS_API_KEY" | jq '.platform_settings.overrides'

# Enable first_message override (keeps the existing custom_llm_extra_body override)
curl -sS -X PATCH "https://api.elevenlabs.io/v1/convai/agents/agent_4501ksad2mf6fw5vdqqzzjcp9n3b" \
  -H "xi-api-key: $ELEVENLABS_API_KEY" -H "Content-Type: application/json" \
  -d '{"platform_settings":{"overrides":{"custom_llm_extra_body":true,"conversation_config_override":{"agent":{"first_message":true}}}}}'
```

If overrides stay disabled, the single-instance history fix still prevents the cold re-greet on its own; the override is just belt-and-suspenders.

### Commits (2026-05-29)
| Commit | Summary |
|---|---|
| `61bc565` | Harden live-session reliability from session triage |
| `ec92d3b` | Fix IQ session stalls, leaked artifacts, and IQ visual rendering |
| `a7adf14` | IQ: hold visual until question resolved, vary order by age, fix teammate diagram + warm-up message |
| `e7441c5` | Fix rep clip upload 413 by uploading directly to Firebase Storage |
| `ec4816d` | Retry transient Gemini server errors (503/5xx/429) in-process |

---

## What landed 2026-05-30 (Track 2)

### Q. Phase 5 multi-agent + GEPA harness — **DONE**

Full journal (baselines, sequencing, learnings):
[`TRACK2-PHASE-JOURNAL.md`](../TRACK2-PHASE-JOURNAL.md).

| Area | What shipped |
|------|--------------|
| **GEPA hang fix** | 24h stall traced to per-turn judge calls + reflection thinking budget + no timeout + ADK null-score crash. `run_optimize.py`, `adk_patches.py`, config knobs. Full run ~13 min, validation 1.0, **seed kept** — do not merge optimizer output (would revert sub-agent prompts). |
| **`vision_coach`** | `peek_camera`, `peek_warmup` split from root (`dc1df1d`). Root transfers on camera ask or repeated verbal setup failure. Default path stays verbal setup. |
| **`drill_coach`** | Scored rep → analysis wait → scorecard → recap tools moved off root (`68c7c11`). Root down to **4 tools**; reconnect rules transfer to `drill_coach` when in drill/recap phase. |
| **Eval baselines** | `make eval-failures` before and after splits — **5/5 passed** both times. Scores in phase journal; raw logs under `evals/baselines/`. |
| **Eval mirror rule** | `evals/agent_module/agent.py` must match `app/agent.py` after every split — evals construct a parallel `root_agent`. |

**Skipped:** memory sub-agent (mid-opening transfer awkward); thin orchestrator-only root.

**Files:** `app/agent.py`, `app/prompts.py`, `evals/agent_module/agent.py`, `evals/run_optimize.py`, `evals/adk_patches.py`, `docs/TRACK2-PHASE-JOURNAL.md`.

### Commits (2026-05-30)
| Commit | Summary |
|---|---|
| `dc1df1d` | GEPA harness + `vision_coach` sub-agent |
| `52f8652` | GEPA null-score patch + in-process optimizer runner |
| `68c7c11` | `drill_coach` sub-agent for scored rep and recap |

### R. Legacy cleanup + doc sync — **DONE**

Audit-driven pass to remove stale code, orphans, and doc drift. **No product behavior change** except removing the obsolete “Next drill” voice chip (single-rep flow).

| Area | What changed |
|------|----------------|
| **Dead UI code** | Removed unused overlay state in `coach/page.tsx`; dropped deprecated `CameraView.recording`; unused lucide imports in `RepScorecard`. |
| **Voice prompts** | Removed “Next drill” chip from `VoiceQuickPrompts` (one scored rep per session). |
| **Drill-id mapping** | Centralised in `apps/buddy-live/src/lib/drills.ts`; `analyze/route.ts` imports it. Keep in sync with `rep_capture._normalize_drill`. |
| **Orphans deleted** | `schema.d.ts`, `make gen-types`, duplicate `marcus_returning_scenario.json`, Next.js boilerplate SVGs, 1.1 MB tracked `.adk/eval_history` JSON. |
| **Gitignore** | `evals/agent_module/.adk/` — eval history stays local. |
| **Docs** | README: 15 tools, single-rep + verbal setup; historical audits/handoffs under `docs/`; `docs/README.md` index; tool/docstring fixes in `warmup_timer.py` / `rep_capture.py`. |

**Still intentional (not removed):** `peek_camera` / `peek_warmup` via `vision_coach` fallback; `coaching.py` static drill dict when Vertex Search misses.

### S. Landing ↔ coach design system + dual-path preview — **DONE**

Commits: `e030ab5` (coach UI alignment), `8760f71` (landing copy + preview).

| Area | What changed |
|------|----------------|
| **Shared tokens** | `globals.css`: `--brand-blue` / `--brand-blue-hover`, `.coach-shell` + `.landing-page` vignette, `.panel-surface`, `.btn-primary`, `.text-brand`. `/` and `/coach` use the same blue CTA and dark panel chrome. |
| **Coach session** | `coach/page.tsx`: safe-area insets, `coach-shell` wrapper, IQ overlay border matches panels; transcript/voice controls use panel surfaces instead of one-off zinc classes. |
| **Landing hero** | Copy covers **both** paths: scored rep scorecard *or* Hockey IQ when off-ice. Beat row: Warm up → Shoot → **Full scorecard**. CTA: **Start practice**. |
| **Landing preview** | `LandingSessionPreview.tsx`: top card mirrors live `RepScorecard` (metrics, bars, strongest/focus); bottom card mirrors `IqVisualCard` (mini rink SVG, breakaway prompt, A/B options). |
| **Open Graph** | `opengraph-image.tsx` tagline matches landing (“shot scorecard — or train your hockey IQ”). |

**Verify on Vercel:** `/` preview cards should resemble `/coach` scorecard + IQ practice without starting a session.

**Files:** `apps/buddy-live/src/app/globals.css`, `page.tsx`, `coach/page.tsx`, `opengraph-image.tsx`, `components/landing/LandingSessionPreview.tsx`, `components/coach/*` (panel/button class pass).

---

## Testing (where to run)

**Policy: Vercel only.** Do not use `npm run dev` / localhost for QA on Buddy Live.

| Environment | When to use |
|---|---|
| **Vercel production** | **All UI and full-stack testing** — [buddy-live-indol.vercel.app/coach](https://buddy-live-indol.vercel.app/coach). Deployment protection on (Buddy Tech login required). Auto-deploys on push to `main`. |
| **Cloud Run ADK** | Always-on brain at `https://buddy-live-adk-7iv2gspkgq-uc.a.run.app` (GitHub Actions on push to `main`). |
| ~~Local `npm run dev`~~ | **Not used** — Firebase admin env not maintained locally; out of scope for this project. |

---

## Open items (superseded 2026-06-07)

This handoff is **historical**. Current remaining work and submission assets:

- [`docs/submission/`](../submission/) — 1-pager, video script, judge toolkit, diagrams
- [`docs/TRACK2-TODOS.md`](../TRACK2-TODOS.md) — only open items (video + Trace screenshot)
- [`docs/ARCHITECTURE.md`](../ARCHITECTURE.md) — deferred ADK 2.0 work (Workflow graph, Memory Bank)

---

## Archive: original open items (2026-05-24)

## Working diagnostic commands

```bash
# Latest ElevenLabs conversations
curl -sS "https://api.elevenlabs.io/v1/convai/conversations?agent_id=agent_4501ksad2mf6fw5vdqqzzjcp9n3b&page_size=5" \
  -H "xi-api-key: $ELEVENLABS_API_KEY"

# Single conversation detail + transcript
curl -sS "https://api.elevenlabs.io/v1/convai/conversations/<CONVERSATION_ID>" \
  -H "xi-api-key: $ELEVENLABS_API_KEY"

# Cloud Run logs (last 10 min)
gcloud logging read 'resource.type="cloud_run_revision" AND resource.labels.service_name="buddy-live-adk"' \
  --project puck-buddy --limit 50 --freshness 10m

# Vercel runtime logs — via MCP get_runtime_logs (projectId=prj_BmstaVv6CgoiHMqMHyrdo0HUaGI4)
```

## Key file map

| Question | File |
|---|---|
| Prompt / session flow | `services/buddy-live-adk/app/prompts.py` |
| peek / framing | `services/buddy-live-adk/app/tools/peek_camera.py` + `apps/buddy-live/src/hooks/usePeekFrameUploader.ts` |
| Recording + analyze | `apps/buddy-live/src/hooks/useRepCapture.ts` + `services/buddy-live-adk/app/tools/rep_capture.py` |
| Landing + design tokens | `apps/buddy-live/src/app/page.tsx`, `globals.css`, `components/landing/LandingSessionPreview.tsx` |
| Conversation UI | `apps/buddy-live/src/app/coach/page.tsx`, `components/coach/*`, `components/camera/*` |
| UX plan + remaining polish | [`UI-CONVERSATION-UX-PLAN.md`](../UI-CONVERSATION-UX-PLAN.md) |
